import React, {Component, PropTypes} from 'react';

export default class OpenButton extends Component {
    static propTypes = {};

    componentDidMount() {
    }

    render() {
        return (
            <div>

            </div>
        );
    }
}
