import React, {Component, PropTypes} from 'react';

export default class Main extends Component {
    static propTypes = {};

    componentDidMount() {
    }

    render() {
        return (
            <div>

            </div>
        );
    }
}
